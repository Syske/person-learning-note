# AI Agent 仓库维护指南

## 仓库概述

个人学习笔记仓库，基于 Obsidian 管理的 Markdown 笔记集合，按技术/主题分类存放。

## 目录结构

笔记按主题分类在顶层目录中，例如 `java/`、`python/`、`docker/`、`go学习笔记/` 等。每个目录下直接存放 `.md` 文件，支持子目录嵌套。

## 日常工作流程

### 1. 拉取最新代码

```sh
git pull
```

如遇 SSH 认证失败，确保 SSH key 已添加到 GitHub。

### 2. 添加新笔记

- 根据笔记主题放到对应顶层目录下
- 如果主题不存在，可新建目录（使用英文或中文均可，参照现有命名风格）
- 文件命名：使用有意义的描述性名称，中英文均可
- 笔记内可包含图片链接（推荐使用阿里云 OSS 图床）

### 3. 修改已有笔记

直接编辑对应 `.md` 文件。

### 4. 提交变更

```sh
git add <修改的文件>
git commit -m "feat: 简短的提交说明"
git push
```

### 5. 更新目录文件

更新 `CONTENTS.md`：

```sh
python markdown-symmary.py
```

提交 `CONTENTS.md` 的变更。

## 冲突处理

遇到合并冲突时：

1. 识别冲突文件：`git status`（标记为 `both modified` 或 `UU` 的）
2. 打开冲突文件，查找 `<<<<<<< HEAD` / `=======` / `>>>>>>>` 标记
3. 保留正确的版本，移除冲突标记
4. 注意嵌套冲突（多次合并遗留的标记）
5. `git add <file>` 标记已解决
6. `git commit` 完成合并

## 敏感信息处理

**严禁**在笔记中包含以下内容：
- 数据库密码、API密钥、Token
- 服务器IP地址、内网地址
- 个人身份信息

如发现已提交的敏感信息，立即用占位符替换（如 `your_password`、`your_host`）。

## 提交规范

使用常规提交格式：
- `feat: 新增xxx笔记` — 添加新内容
- `fix: 修复xxx问题` — 修正错误
- `docs: 更新xxx文档` — 文档更新
- `chore: xxx` — 杂项维护

## 注意事项

- 不要提交 `.obsidian/` 目录（已在 `.gitignore` 中排除）
- 不要提交二进制文件、大文件
- 图床图片无需提交，链接即可
- 保持目录整洁，不要随意创建顶层目录
