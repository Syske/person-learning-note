
#### mycli

`mycli` 是一个 `MySQL` 的命令行客户端，可以实现自动补全（`auto-completion`）和语法高亮。

官网：[mycli](https://www.mycli.net/)

##### 安装

```shell
pip install mycli
```

#### iredis

`iredis`是一个`redis`的命令行客户端，可以实现自动补全和语法高亮
官网：[iredis](https://github.com/laixintao/iredis)

##### 安装

```sh
pip install iredis
```